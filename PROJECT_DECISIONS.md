# YourEnglishSucks - 專案決策與 AI 工作流備忘錄 (Project Decisions & AI Context)

> **【AI 工作流必讀事項】**
> 本檔案記錄所有經由與使用者討論確認的產品規格、設計決策與互動邏輯。
> 任何 AI 助手在接手或後續開發此專案時，**必須優先讀取並遵循本文件規範**。

---

## 📌 核心架構與設計決策清單 (Confirmed Decisions)

### 1. 運行環境與資料庫拓撲
- **資料庫 (DB)**：使用本機 Docker 運行容器化 PostgreSQL 16 (`docker-compose.yml`)。
- **後端 (Spring Boot)**：先在 Host 本地直接運行，透過 `localhost:5432` 串接容器化的 PostgreSQL。
- **Gemini API Key**：配置於後端 `application.yaml`（讀取環境變數 `${GEMINI_API_KEY:}`），同時前端設定頁面保留手動輸入覆蓋的彈性。
- **預設模型 (Model)**：直接 hardcoded 使用最新 `gemini-2.5-flash` 作為預設主力模型。

### 2. 標題 (Title) 與雙向重新命名邏輯 (確認選項 C)
- **初次生成**：首次修飾成功後，預設擷取原始英文文本的前 35 個字元作為對話標題。
- **雙向手動修改 (選項 C)**：
  - **左側邊欄**：項目懸停時提供「✏️ 重新命名」按鈕，可原地切換為 Input 編輯並儲存。
  - **中央頂部標題列**：中央主視窗頂端同步顯示當前標題，點擊標題亦可就地編輯，兩處即時雙向連動同步。

### 3. 草稿保存與強制停止生成機制 (確認選項 B + 停止功能)
- **草稿暫存 (選項 B)**：使用者未送出的文字會按 Session 自動保存在前端狀態/記憶體中，切換不同對話或新對話時草稿不會遺失。
- **強制停止按鈕 (Stop Button)**：
  - 當 AI 正在思考/生成時，介面提供醒目的「⏹️ 停止生成 (Stop)」按鈕。
  - 點擊後立即中斷請求，並**自動將原本發送的文本無損還原回輸入欄**，供使用者重新編輯或重新發問。

### 4. 文本長度上限與逾時設定
- **原始文本長度上限**：單次英文修飾文本上限為 **4,000 字元**（介面即時顯示字數統計與剩餘額度）。
- **後端連線逾時**：設定為 **60 秒**，以因應長篇文章分析。

### 5. 追加發問上下文攜帶策略 (確認選項 B)
- **滑動窗口策略**：每次追加提問時，永久保留「Round 1 原始文本 + Round 1 修飾結果」作為核心基準，並搭配「最近 6~8 輪追加問答」一同送入 Gemini，既維持對原文修飾的深度討論，又有效控制 Token 成本。

### 6. 刪除對話防呆機制 (確認選項 A)
- 點擊左側「🗑️ 垃圾桶」時，彈出確認對話框（「確定要刪除這筆紀錄嗎？此動作無法復原」），確認後才進行刪除。

### 7. 錯誤處理與重試機制
- **錯誤呈現**：當後端連線異常或 Gemini API 失敗時，聊天視窗顯示友善的錯誤提示卡片。
- **點擊重試**：卡片上提供「點擊重試 🔄」按鈕，使用者點擊即可重新觸發該輪請求，無需重新輸入文本。

### 8. 英文修飾 Skill 輸出結構規範 (2026 最新確認)
- **注入時機**：**僅在第 1 次輸入時由後端自動注入**。第 2 次及之後的追加發問，保持同一對話歷史上下文 (Session Context)，不再重複帶入此 Skill。
- **排版順序與結構**：
  1. **第一段：🌟 修飾後英文全文 (Refined English Text)**：置頂輸出，提供「🗣️ 自然道地口語版」與「💼 專業商務正式版」，方便第一時間完整閱讀或複製。
  2. **第二段：🔍 語病與道地性解析 (Chinglish & Grammar Analysis)**：精準條列引述原文生硬或文法問題。
  3. **第三段：💡 修改原因與語境解析 (Rationale & Insights)**：條列解說用詞、時態與語境差異。
  4. **第四段：📚 實用道地片語與延伸替換 (Idioms & Upgrades)**：附中文意涵與例句。
- **負向約束 (Negative Constraints)**：
  - **嚴禁開場廢話**：不可出現「你好！我是你的教練...」、「這段文字意思是清晰的...」、「以下為你進行詳細的拆解...」等問候前言。
  - **嚴禁結尾客套**：不可出現「希望這些建議對你有幫助...」、「繼續加油！😊」等收尾句。
  - **全程直接條列**：簡潔有力、直奔主題。

### 9. 輸入框與鍵盤快速鍵行為 (全程一致)
- **Enter 鍵**：跳下一行（換行），並觸發輸入框高度自動向下延伸 (`auto-expanding textarea`)。
- **Shift + Enter 鍵** 或 **點選送出按鈕**：送出文本。
- **歷史載入**：左側邊欄與中央視窗歷史紀錄均一次載入全部。
