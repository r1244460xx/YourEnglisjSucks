使用情境
1. 我要設計一個幫助自己學習英文，跟ai agent互動的服務，使用者介面像是市面上的chatgpt或gemini web，但細節上有很大的不同，底層使用gemini key串接gemini model
2. 功能分成三大類英文修飾、英翻中、中翻英，可以設計成一個水平tabs來切換功能
3. 英文修飾功能
a. 點選左側導覽欄位的"新對話"之後，產生的唯一一個輸入欄位是要被修飾的英文文本，因為是文本輸入欄位可以是一行 但是按enter之後可以一直跳下一行 並且輸入欄位要能一直往下延伸，最後要按Shirt+Enter或是點選送出按鈕才會把文本送出
b. 送出之後，後端會把文本作為我為了這個功能特別設計的skill來輸出答案，重點是只有第一次的輸入會需要搭配skill，後端收到ai給的答案之後傳給前端
c. 前端顯示後端返回的ai答案之後，用現在agent web對話的gui來顯示，此時文本輸入欄位不會消失，開放給使用者追加發問，是為了針對同一個session and context做追加英文上的問答，所以第二次以後的輸入不需要帶入skill，直接輸入給AI，並用對話顯示ai的答覆就好
d. 大原則是一次新對話只能使用一次英文文本修飾，同一個對話下面只能對該文本修飾做討論
e. 請設計適當的schema儲存每一次的來回對話，另外第一次的文本作為輸入要多額外存在一個獨立的table，做為日後分析使用者習慣之用
f. 進入這個tab時，參考市面上agent web顯示歷史對話的方式，先顯示數筆資料，滑鼠往下滑歷史資料到底時，再額外從資料庫載入更舊的資料
4. 先實作"英文修飾"就好，其他兩個功能的UI其實大同小異
5. 需要網頁做為前端，SpringBoot做為後端，你來評估需要那些資料適合儲存這些資料，有需要關聯式資料庫的話使用PostgreSQL，有其他功能用的資料庫則不預設立場讓你決定
6. 先給我你預期的網頁layout，data model，使用者情境細節，流程圖，以及提出你的任何use cases疑問讓我來補充